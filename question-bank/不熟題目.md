## question-bank/by-order_v1
* Q100 權限管理細節太多，還沒背起來
* Q105 對 MLflow 語法不熟
* Q106 認知不足，看題慢，無法好好寫題

## question-bank/by-order_b2
* Q-003 當 Structured Streaming 查詢沒有明確指定 `trigger` 選項時，Spark 會使用預設的觸發間隔：**500 毫秒
* Q-007 CDF 有 _change_data
* Q-009 Cron 是一種時間表示語法，源自 Unix 系統，用於定義任務的執行時間。在 Databricks Jobs 中，Cron syntax 用來定義複雜的排程規則。
* Q-010 Deletion Vectors 是 Delta Lake 中的一種元資料結構，用於追蹤資料檔案中已邏輯刪除的列，而不需要實際重寫檔案。
* Q-11 WITH HISTORY, Temporary credentials, Direct storage access
* Q-13 streaming: Unlimited Retries, 1 Maximum Concurrent Run
* Q-16 SET TBLPROPERTIES ('delta.deletedFileRetentionDuration' = 'interval 30 days');
* Q-17 `_change_data` 資料夾中的檔案與表格的主要資料檔案遵循相同的保留政策 - 當執行 `VACUUM` 時，過期的 CDF 資料也會被刪除
* Q-018 shallow clone 只複製 transaction log, vacuum 會造成 files not found
* Q-020 `microBatchDF.sparkSession.sql(sql_query)`，在 Executor 取 spark session  
* Q-021 Repair Run 的設計**:允許重新執行失敗的任務，可以在修復時調整參數，修改只影響當前這次修復執行
* Q-23 **rescue 模式的行為**: - Schema **不會演進**（保持穩定） - 新欄位的資料會被放入 `_ data` 欄位 - 串流**不會失敗**
* Q-029  ALTER TABLE <table_name> SET ROW FILTER <function_name> ON (<column_list>);
* Q-030 Send GET request to the endpoint 'api/2.2/jobs/runs/get'

## question-bank/by-order_b3
* Q-034 `details:flow_progress.data_quality.expectations` LDP pipeline Event Log Query
* Q-038 CONSTRAINT valid_id EXPECT (id IS NOT NULL) ON VIOLATION DROP ROW
* Q-039 高基數欄位的統計資訊效用有限:Nested fields do not count when determining the first 32 columns in the table.
* Q-041 Integration Testing 測試模組間的整合與互動
  Q-047 權限設定在 scope 層級，不是個別 secret 層級
  Q-058 - `spark.read` 是靜態（batch）讀取，startingVersion: 0` 表示從頭開始讀，次執行都讀取從版本 0 到最新的所有變更，mode("overwrite")` 覆蓋目標表
 

## question-bank/by-orkder_b4
* Q-003 使用 Databricks REST API mistake 2 POST requests (會有兩筆 job_id)
* Q-006 `WHEN is_member("instructors") THEN TRUE`**   
* Q-008 D.  | 角色/權限 | 可讀 secrets: Workspace Admin，Secret Creator，READ / MANAGE
* Q-009 `delta.appendOnly = true` 會 **禁止 UPDATE 與 DELETE**，但仍允許 INSERT
* Q-011 # Databricks notebook source
* Q-012 **B.** .option("ignoreDeletes", True)
* Q-028  ALTER TABLE customer_accounts ALTER COLUMN credit_card SET MASK card_mask;
* Q-029 trigger(processingTime="2 minutes") 會讓 Structured Streaming 查詢每 2 分鐘執行一次 micro-batch 
* Q-036 SELECT * FROM daily_activities EXCEPT SELECT * FROM daily_activities@v{current_version-1}
* Q-039 .option("cloudFiles.schemaEvolutionMode", "addNewColumns")
* Q-042 SELECT CASE
  WHEN new_tickets > 200 AND under_processing > 150 THEN 1
    ELSE 0
  END
FROM (
  SELECT
    SUM(CASE WHEN status = "new" THEN 1 ELSE 0 END) AS new_tickets,
    SUM(CASE WHEN status = "in_progress" THEN 1 ELSE 0 END) AS under_processing
  FROM tickets
) statistics