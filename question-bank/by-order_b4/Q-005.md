# Question #005

---

## 題目資訊
### 題目編號
**ID:** `Q-005`
### 來源
**來源:** Community
### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A junior data engineer creates a Databricks job with 15 notebook tasks, each performing the same data validation logic on 15 different tables. Each task depends on the completion of the previous one, making the workflow long and difficult to maintain.

What would be a more efficient and scalable solution for this use case?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** Use a foreach task to run the same validation notebook for each table in parallel, passing the table name as a parameter
- **B.** Schedule 15 separate jobs instead of having multiple tasks in one job
- **C.** Configure the 15 notebook tasks to run in parallel, each with a separate cluster configuration
- **D.** Combine all table validations into one large notebook and loop through all tables sequentially

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Databricks-Workflows`
### Trap Tags (陷阱類型標籤)
**Traps:** `Performance-Misconception`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Development

---

## 答案與來源
### 正確答案
**正解:** `A`
### 答案來源
- **來源標註答案:** A
- **社群共識:** A

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Databricks Workflows - For Each Task
**關鍵概念:** 參數化重用、平行化執行、可維護性
**題目關鍵字：**
- **same validation logic**: 同一個 notebook 重用
- **depends on the completion**: 不必要的序列依賴
- **efficient and scalable**: 需要平行化與參數化

---

## ✅ 正解說明
### 為什麼 A 是正確答案？
For Each Task 可以讓同一個 notebook 以不同參數重複執行，並可設定平行度。對多表同邏輯的驗證而言，**參數化 + 平行化**能大幅縮短流程時間並降低維護成本，因此 A 最符合「效率」與「可擴展」需求。

---

## ❌ 錯誤選項排除
### B. 拆成 15 個 jobs
**為什麼錯誤？**
- 增加管理成本，且無法集中監控與統一參數管理。

### C. 平行 15 個 tasks 且不同 cluster
**為什麼錯誤？**
- 雖可平行，但維護成本更高且不必要；For Each 已能重用同一 task 邏輯。

### D. 單一 notebook 逐一跑
**為什麼錯誤？**
- 仍是序列執行，延長耗時，與「更有效率」相反。

---

## 🧠 記憶法
### 口訣
**「同邏輯多資料，For Each 最省力。」**

### 比較表
| 方案 | 維護性 | 速度 | 擴展性 |
|---|---|---|---|
| For Each Task | ✅ 高 | ✅ 快 | ✅ 高 |
| 多 job | ❌ 低 | ⚠️ 中 | ⚠️ 中 |
| 多 cluster 平行 | ⚠️ 中 | ✅ 快 | ❌ 成本高 |
| 單 notebook 迴圈 | ✅ 高 | ❌ 慢 | ⚠️ 中 |

---

## 🔧 For Each Task 使用方式

### 基本概念
For Each Task 是 Databricks Workflows 中的控制流 task，可對一組輸入項目**迭代執行**同一個嵌套 task（nested task）。

### 設定步驟
1. **建立 For Each Task**：在 Job 中新增 task，選擇 `For each` 類型
2. **定義輸入陣列**：提供要迭代的項目清單（JSON array）
3. **設定嵌套 Task**：指定要重複執行的 notebook/task
4. **參數傳遞**：使用 `{{input}}` 取得當前迭代值

### 輸入格式範例
```json
["table_a", "table_b", "table_c"]
```
或物件陣列：
```json
[
  {"table": "sales", "schema": "bronze"},
  {"table": "orders", "schema": "bronze"}
]
```

### 在 Nested Notebook 中取得參數
```python
# 取得當前迭代的值
table_name = dbutils.widgets.get("table_name")

# 如果傳入的是物件，可透過 task values 取得
# {{input.table}} 和 {{input.schema}}
```

### Concurrency 設定
- **預設**：迭代項目**平行執行**
- **max_concurrency**：可限制同時執行的數量（例如設為 5，一次最多跑 5 個）
- 設為 `1` 則變成**序列執行**

### UI 設定位置
```
Job → Add Task → For each
├── Inputs: 輸入的 JSON array（或引用上游 task value）
├── Concurrency: 平行度上限
└── Nested Task: 選擇要執行的 notebook 並設定參數映射
```

### 實際應用情境
| 情境 | 輸入範例 |
|------|----------|
| 多表驗證 | `["users", "orders", "products"]` |
| 多環境部署 | `["dev", "staging", "prod"]` |
| 分區處理 | `["2024-01", "2024-02", "2024-03"]` |

---

## 📚 官方文件
- [For each task](https://docs.databricks.com/en/workflows/jobs/for-each-task.html)

---

**[返回題目](#question-005)**
