# Q-085

## 題目資訊

**ID:** `Q-085`  
**來源:** Official Practice Exam  
**難度:** `L2-Intermediate`

---

## 題目內容

### 題幹

A distributed team of data analysts share computing resources on an interactive cluster with autoscaling configured. In order to better manage costs and query throughput, the workspace administrator is hoping to evaluate whether cluster upscaling is caused by many concurrent users or resource-intensive queries.

In which location can one review the timeline for cluster resizing events?

### 選項

- **A.** Workspace audit logs
- **B.** Driver's log file
- **C.** Ganglia
- **D.** Cluster Event Log
- **E.** Executor's log file

---

## 標籤系統

**Topics:** `Cluster-Management`, `Databricks-Workflows`  
**Traps:** `Monitoring-Tool-Confusion`  
**Domain:** `Operations & Automation`

---

## 官方答案與社群討論

**官方標註答案:** C  
**實際正確答案:** D  
**社群共識:** D（官方答案錯誤）

---

## 📍 考點識別

### 主要考點
**核心技術:** Databricks Cluster 監控與事件追蹤  
**知識領域:** 維運與自動化（Operations & Automation）  
**關鍵概念:** 
- Cluster Event Log 的用途與內容
- Autoscaling 事件追蹤
- 監控工具的功能差異（Event Log vs Ganglia vs Audit Logs）

### 次要考點
- Cluster 自動擴展（Autoscaling）機制
- 成本管理與效能監控
- Databricks 監控工具生態系統

---

## ✅ 正解說明

### 為什麼 D (Cluster Event Log) 是正確的？

**技術原理:**

**Cluster Event Log** 是 Databricks 專門用來記錄叢集生命週期事件的工具：

```
Cluster Event Log 記錄的事件包括：
✅ Cluster Started       - 叢集啟動
✅ Cluster Terminated    - 叢集終止
✅ Upsize Completed      - 擴容完成 ← 題目要找的！
✅ Downsize Completed    - 縮容完成 ← 題目要找的！
✅ Node Added            - 節點新增
✅ Node Removed          - 節點移除
✅ Driver Not Responding - Driver 無回應
✅ Configuration Changed - 配置變更
```

**在哪裡找到 Cluster Event Log？**

1. **Databricks UI 路徑：**
   ```
   Compute → [選擇叢集] → Event Log 標籤
   ```

2. **顯示內容範例：**
   ```
   Timestamp                | Event Type         | Details
   -------------------------+--------------------+---------------------------
   2024-01-15 10:23:45 UTC | Upsize Completed   | Added 2 workers (4→6)
   2024-01-15 10:15:32 UTC | Starting Resize    | Target: 6 workers
   2024-01-15 09:45:12 UTC | Cluster Started    | 4 workers initialized
   ```

**符合需求:**

題目需求：「評估叢集擴容（upscaling）是由多併發使用者還是資源密集型查詢引起」

✅ **Cluster Event Log 提供：**
- 精確的 resizing 時間點（timeline）
- 擴容/縮容的觸發時間
- 節點數量變化記錄
- 可對應到使用者活動或查詢執行時間

**實務應用:**

```python
# 使用 Databricks REST API 取得 Cluster Events
import requests

cluster_id = "1234-567890-abc123"
events = requests.get(
    f"https://<databricks-instance>/api/2.0/clusters/events",
    headers={"Authorization": f"Bearer {token}"},
    json={
        "cluster_id": cluster_id,
        "order": "DESC",
        "event_types": ["UPSIZE_COMPLETED", "DOWNSIZE_COMPLETED"],
        "limit": 100
    }
)

# 分析擴容事件
for event in events.json()["events"]:
    print(f"{event['timestamp']}: {event['type']} - {event['details']}")
```

---

## ❌ 錯誤選項排除

### 選項 A：Workspace Audit Logs

**錯誤原因:**

Audit Logs 記錄的是「誰做了什麼」（使用者操作），不是「系統自動事件」：

```
Audit Logs 記錄內容：
✅ 使用者登入/登出
✅ 建立/刪除叢集（人為操作）
✅ 權限變更
✅ Notebook 執行記錄
✅ Secrets 存取記錄

❌ 不記錄：自動擴容事件（autoscaling 是系統自動觸發）
```

**概念差異:**
- **Audit Logs** = 稽核日誌（合規性、安全性）
- **Event Log** = 事件日誌（運維、故障排除）

**查看位置:**
```
Admin Console → Audit Logs
或使用 API：
GET /api/2.0/workspace/audit-logs
```

---

### 選項 B：Driver's Log File

**錯誤原因:**

Driver Log 記錄的是「應用程式執行日誌」，不是「叢集管理事件」：

```
Driver Log 內容：
✅ Spark job 執行細節
✅ DataFrame 轉換日誌
✅ SQL 查詢執行計畫
✅ 應用程式錯誤訊息
✅ 自訂的 print/logging 輸出

❌ 不包含：叢集擴容/縮容事件
```

**為什麼不適合:**
- Driver Log 關注「應用層」，不關注「基礎設施層」
- 看不到 autoscaling 的觸發時機和節點變化

**查看位置:**
```
Compute → [叢集] → Driver Logs 標籤
或 Spark UI → Executors → Driver
```

---

### 選項 C：Ganglia（官方錯誤答案）

**為什麼官方答案錯誤:**

**Ganglia 是什麼？**
- 開源的分散式監控系統
- Databricks 用來顯示「即時效能指標」

**Ganglia 顯示的內容：**
```
✅ CPU 使用率（即時圖表）
✅ Memory 使用量（即時圖表）
✅ Network I/O（即時圖表）
✅ Disk I/O（即時圖表）

❌ 不顯示：
   - 歷史事件時間軸（timeline）
   - 明確的 resizing 事件記錄
   - 擴容/縮容的具體時間點
```

**Ganglia vs Event Log 對比：**

| 功能 | Ganglia | Cluster Event Log |
|-----|---------|-------------------|
| **目的** | 即時效能監控 | 事件歷史記錄 |
| **顯示方式** | 圖表（metrics） | 列表（events） |
| **時間粒度** | 連續的時間序列 | 離散的事件點 |
| **Resizing Events** | ❌ 只能間接推測 | ✅ 明確記錄 |
| **查看位置** | Metrics 標籤 | Event Log 標籤 |

**為什麼容易混淆:**
- Ganglia 可以「推測」擴容時機（看到節點數突然增加）
- 但無法提供「明確的 resizing 事件時間軸」（題目要求）
- Event Log 才是官方推薦的事件追蹤工具

**實務範例：**

如果你在 Ganglia 看到：
```
10:00 - CPU: 80%, Memory: 70%, Workers: 4
10:15 - CPU: 85%, Memory: 90%, Workers: 4  ← 負載增加
10:20 - CPU: 60%, Memory: 65%, Workers: 6  ← 節點數增加了？
```

你只能「猜測」10:15-10:20 之間可能發生擴容，但：
- ❌ 不知道確切觸發時間
- ❌ 不知道擴容決策的原因
- ❌ 沒有事件的明確記錄

在 Cluster Event Log 看到：
```
10:17:32 - Starting Resize: Target 6 workers (reason: high CPU load)
10:18:45 - Upsize Completed: Added 2 workers (4→6)
```
- ✅ 明確的時間點
- ✅ 明確的事件類型
- ✅ 清楚的變化細節

---

### 選項 E：Executor's Log File

**錯誤原因:**

Executor Log 記錄「任務執行細節」，不是「叢集管理事件」：

```
Executor Log 內容：
✅ Task 執行日誌
✅ Shuffle 操作記錄
✅ 資料分區處理
✅ Stage 執行細節

❌ 不包含：叢集層級的擴容事件
```

**概念差異:**
- Executor = 執行任務的 worker 節點
- 每個 executor 只知道自己的任務，不知道叢集全局事件

---

## 🧠 記憶法與最佳實踐

### 口訣：「Event Log 看事件，Ganglia 看指標，Audit 看操作」

| 工具 | 用途 | 查看內容 | 使用場景 |
|-----|------|---------|---------|
| **Cluster Event Log** | 📋 事件記錄 | 啟動、終止、擴容、縮容 | 找出叢集變化時間點 |
| **Ganglia** | 📊 效能監控 | CPU、Memory、I/O 圖表 | 即時效能分析 |
| **Audit Logs** | 🔒 稽核日誌 | 使用者操作、權限變更 | 合規性、安全性調查 |
| **Driver/Executor Logs** | 🐛 應用日誌 | 程式執行、錯誤訊息 | 除錯應用程式 |

### Databricks 監控工具生態系統

```
監控層級                    工具                   查看位置
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
使用者操作層              Audit Logs             Admin Console
  ↓
叢集生命週期層     ┌──→ Cluster Event Log      Compute → Event Log
  ↓                │
效能監控層         └──→ Ganglia (Metrics)      Compute → Metrics
  ↓
應用程式層              Driver/Executor Logs    Compute → Logs
  ↓
Spark 內部層            Spark UI                Compute → Spark UI
```

### 查找 Cluster Resizing Events 的完整流程

```python
# 步驟 1：查看 Cluster Event Log（最直接）
# UI 路徑：Compute → [叢集名稱] → Event Log 標籤
# 過濾：UPSIZE_COMPLETED, DOWNSIZE_COMPLETED

# 步驟 2：分析觸發時間與使用者活動的關聯
# 對照 Query History 或 Notebook 執行記錄

# 步驟 3：使用 Ganglia 查看當時的資源使用狀況
# 確認是 CPU、Memory 或 Network 觸發了擴容

# 步驟 4：調整 Autoscaling 配置（如果需要）
{
  "autoscale": {
    "min_workers": 2,
    "max_workers": 8
  },
  "autotermination_minutes": 30
}
```

### 實務建議：成本與效能分析

**情境：如題目所述，想知道擴容是否由「多併發使用者」或「資源密集查詢」引起**

**分析步驟：**

1. **從 Event Log 取得擴容時間點**
   ```
   2024-01-15 10:17:32 - Upsize Completed (4→6 workers)
   2024-01-15 14:23:11 - Upsize Completed (6→8 workers)
   ```

2. **查看當時的 Query History**
   ```sql
   -- 在 Databricks SQL 查詢
   SELECT query_text, user_name, execution_time, start_time
   FROM system.query.history
   WHERE start_time BETWEEN '2024-01-15 10:15:00' AND '2024-01-15 10:20:00'
   ORDER BY execution_time DESC;
   ```

3. **對照 Ganglia Metrics**
   - 檢查 CPU/Memory 是否有特定查詢造成的尖峰
   - 確認是否有多個併發查詢

4. **得出結論並優化：**
   - 如果是單一重查詢 → 優化該查詢（加索引、調整計畫）
   - 如果是多併發 → 考慮增加 min_workers 或使用 SQL Warehouse

---

## 📚 延伸閱讀

### 官方文件
1. [Cluster Event Logs](https://docs.databricks.com/clusters/clusters-manage.html#view-cluster-event-logs)
2. [Monitor Cluster Performance (Ganglia)](https://docs.databricks.com/clusters/clusters-manage.html#monitor-performance)
3. [Audit Logging](https://docs.databricks.com/administration-guide/account-settings/audit-logs.html)
4. [Autoscaling](https://docs.databricks.com/clusters/configure.html#autoscaling)

### REST API 參考
```bash
# 取得 Cluster Events
curl -X POST https://<databricks-instance>/api/2.0/clusters/events \
  -H "Authorization: Bearer <token>" \
  -d '{
    "cluster_id": "1234-567890-abc123",
    "event_types": ["UPSIZE_COMPLETED", "DOWNSIZE_COMPLETED"],
    "limit": 50
  }'

# 回傳範例
{
  "events": [
    {
      "cluster_id": "1234-567890-abc123",
      "timestamp": 1642334252000,
      "type": "UPSIZE_COMPLETED",
      "details": {
        "previous_workers": 4,
        "target_workers": 6,
        "reason": "High CPU load"
      }
    }
  ]
}
```

---

## 🎯 考試建議

**快速判斷法：題目關鍵字 → 對應工具**

| 題目關鍵字 | 正確答案 | 快速記憶 |
|----------|---------|---------|
| **resizing events**, **timeline** | Cluster Event Log | 事件=Event Log |
| **real-time performance**, **CPU/Memory** | Ganglia | 即時=Ganglia |
| **user operations**, **compliance** | Audit Logs | 操作=Audit |
| **application errors**, **debugging** | Driver/Executor Logs | 除錯=應用日誌 |

**記憶重點：**
1. ✅ **Cluster Event Log** = 叢集生命週期事件（啟動、終止、擴容、縮容）
2. ✅ **Ganglia** = 即時效能指標（CPU、Memory、I/O 圖表）
3. ✅ 題目明確要「timeline」和「resizing events」→ 只有 Event Log 提供

**陷阱識別：**
- ⚠️ Ganglia 可以「看到」節點數變化，但無法提供「事件時間軸」
- ⚠️ Audit Logs 記錄「人為操作」，不記錄「自動擴容」
- ⚠️ Driver/Executor Logs 是「應用層」，看不到「叢集層」事件

**核心觀念：**
✅ 官方答案 C (Ganglia) 錯誤，正確答案是 D (Cluster Event Log)  
✅ Event Log 提供明確的事件記錄，Ganglia 只提供指標圖表  
✅ 不同監控工具有不同用途，要根據需求選擇正確工具
