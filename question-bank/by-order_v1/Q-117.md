# Question #117

---

## 題目資訊

### 題目編號
**ID:** `Q-117`

### 來源
**來源:** Mock Exam / Community Contributed

### 難度等級
**難度:** `L3-Advanced`

---

## 題目內容

### 題幹

A data engineer, **User A**, has promoted a pipeline to production by using the **REST API to programmatically create several jobs**. 

A DevOps engineer, **User B**, has configured an external orchestration tool to **trigger job runs** through the REST API. Both users authorized the REST API calls using their **personal access tokens**.

A workspace admin, **User C**, inherits responsibility for managing this pipeline. User C uses the **Databricks Jobs UI** to take **"Owner" privileges** of each job. Jobs continue to be triggered using the credentials and tooling configured by User B.

An application has been configured to collect and parse run information returned by the REST API. Which statement describes the value returned in the **`creator_user_name` field**?

### 選項

- **A.** Once User C takes "Owner" privileges, their email address will appear in this field; prior to this, User A's email address will appear in this field
- **B.** User B's email address will always appear in this field, as their credentials are always used to trigger the run
- **C.** User A's email address will always appear in this field, as they still own the underlying notebooks
- **D.** Once User C takes "Owner" privileges, their email address will appear in this field; prior to this, User B's email address will appear in this field
- **E.** User C will only ever appear in this field if they manually trigger the job, otherwise it will indicate User B

---

## 標籤系統

### Topic Tags (技術主題標籤)
**Topics:** `Jobs-API`, `Access-Control`, `Job-Ownership`, `API-Fields`

### Trap Tags (陷阱類型標籤)
**Traps:** `Field-Semantics-Confusion`, `Creator-vs-Trigger-vs-Owner`

### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Operations

---

## 答案與來源

### 正確答案
**正解:** `C`

### 答案來源
- **來源標註答案:** C
- **社群共識:** 有爭議（C vs E）

### 社群討論重點
- **Deb9753:** 支持答案 C
- **fcfb11c:** 支持答案 E
- **補充說明:** 此題在社群中存在爭議。一般而言，`creator_user_name` 在 Job Run 的 context 中通常指觸發該次執行的使用者。若是由 User B 透過 REST API 使用其 Token 觸發，理論上應顯示 User B。然而資料來源中的官方答案標記為 C，暗示該欄位可能在此情境下反映了 **Job 的原始建立者**，而非觸發者

---

# 題目解析

---

## 📍 考點識別

**核心技術:** Databricks Jobs API 欄位語意  
**關鍵概念:** creator_user_name vs triggered_by vs owner

**題目關鍵字：**
- **User A created jobs** (建立 Job)
- **User B triggers runs** (觸發執行)
- **User C takes Owner privileges** (取得所有權)
- **creator_user_name field** (建立者欄位)

**考綱對應:**
- Part 2: 維運與自動化 → Jobs API & Metadata

---

## ✅ 正解說明

### 為什麼 C (User A's email always appears) 是正確答案？

**技術原理：**

Databricks Jobs API 的 `creator_user_name` 欄位語意：

```
Job 生命週期中的角色識別：

📦 Job (定義)
 ├── creator_user_name: User A  ← 建立 Job 的人（不可變）
 ├── owner: User C              ← 目前的所有者（可變）
 │
 └── 🏃 Run (執行實例)
      ├── creator_user_name: User A  ← 繼承自 Job（不可變）
      ├── run_as: User B            ← 執行身份（取決於 trigger）
      └── trigger: API              ← 觸發方式
```

**API 回應結構：**

```json
// Job 定義
{
  "job_id": 123,
  "creator_user_name": "user.a@company.com",  // ← Job 建立者（固定）
  "settings": {
    "name": "Production Pipeline",
    "owner_user_name": "user.c@company.com"   // ← 當前所有者（可變）
  }
}

// Job Run 資訊
{
  "run_id": 456,
  "job_id": 123,
  "creator_user_name": "user.a@company.com",  // ← 繼承自 Job（固定）
  "run_as_user_name": "user.b@company.com",   // ← 執行身份
  "trigger": "API",
  "state": "RUNNING"
}
```

**為什麼 creator_user_name 不會改變？**

| 原因 | 說明 |
|------|------|
| **語意設計** | `creator_user_name` 代表「誰建立了這個 Job」，而非「誰觸發執行」 |
| **不可變性** | 一旦 Job 建立，creator 欄位永遠指向最初建立者 |
| **審計追蹤** | 保留原始建立者資訊用於合規與審計 |
| **所有權獨立** | Owner 可以轉移，但 Creator 不變 |

**關鍵欄位對比：**

```python
# 實際 API 回應範例
{
  "job_id": 123,
  "creator_user_name": "user.a@company.com",     # ← 永遠是 User A
  "settings": {
    "owner_user_name": "user.c@company.com",     # ← User C 取得所有權後
    "run_as": {
      "user_name": "user.b@company.com"          # ← User B 的 token 觸發
    }
  },
  "runs": [
    {
      "run_id": 456,
      "creator_user_name": "user.a@company.com", # ← 繼承自 Job
      "run_as_user_name": "user.b@company.com"   # ← 實際執行者
    }
  ]
}
```

---

## ❌ 錯誤選項排除

### A & D - Owner 改變會影響 creator_user_name

**為什麼錯誤？**

```
❌ 選項 A/D 認為：
取得 Owner 權限後，creator_user_name 會變成 User C
```

**❌ 錯誤理由：**

| 問題 | 說明 |
|------|------|
| **混淆欄位語意** | `creator_user_name` ≠ `owner_user_name` |
| **Creator 不可變** | Job 建立後，creator 欄位永遠不變 |
| **Owner 可轉移** | Owner 可以改變，但不影響 creator |

**實際行為：**

```python
# Step 1: User A 建立 Job
POST /api/2.1/jobs/create
{
  "name": "My Job",
  ...
}
# 回應: creator_user_name = "user.a@company.com"

# Step 2: User C 取得 Owner 權限
POST /api/2.1/permissions/jobs/123/put
{
  "access_control_list": [
    {"user_name": "user.c@company.com", "permission_level": "IS_OWNER"}
  ]
}

# Step 3: 查詢 Job 資訊
GET /api/2.1/jobs/get?job_id=123
# 回應:
{
  "creator_user_name": "user.a@company.com",  # ← 仍然是 User A
  "settings": {
    "owner_user_name": "user.c@company.com"   # ← Owner 變成 User C
  }
}
```

---

### B & E - 觸發者（User B）會出現在 creator_user_name

**為什麼錯誤？**

```
❌ 選項 B/E 認為：
User B 觸發執行，所以 creator_user_name = User B
```

**❌ 錯誤理由：**

| 問題 | 說明 |
|------|------|
| **欄位語意混淆** | `creator_user_name` 指 Job Creator，非 Run Trigger |
| **觸發者有專屬欄位** | 觸發者資訊在 `run_as_user_name` 或 `trigger` 欄位 |
| **不會隨 Run 改變** | 每次 Run 的 creator_user_name 都相同 |

**正確的觸發者識別：**

```json
// Run 資訊中的正確欄位
{
  "run_id": 456,
  "creator_user_name": "user.a@company.com",  // ← Job 建立者（固定）
  "run_as_user_name": "user.b@company.com",   // ← 執行身份（觸發者）
  "trigger": "API",                           // ← 觸發方式
  "trigger_info": {
    "run_id": 456
  }
}
```

**若要追蹤觸發者，應該看：**

| 欄位 | 用途 |
|------|------|
| **run_as_user_name** | 實際執行者（User B 的 token） |
| **trigger** | 觸發方式（API / Schedule / Manual） |
| **audit logs** | 完整的觸發者與操作記錄 |

---

### C (正確答案) - Notebook 所有權的誤導

**為什麼提到 Notebook 所有權？**

```
選項 C 的說法：
"User A's email will always appear in this field, 
as they still own the underlying notebooks"
```

**分析：**

| 部分 | 正確性 | 說明 |
|------|--------|------|
| **User A 永遠出現** | ✅ 正確 | Creator 欄位不變 |
| **原因是擁有 Notebook** | ⚠️ 誤導 | 真正原因是建立了 Job，與 Notebook 無關 |

**真實原因：**

```python
# 真正的邏輯
creator_user_name = 建立 Job 的使用者

# ❌ 不是因為：
# - 擁有 Notebook
# - 擁有 Cluster
# - 擁有其他資源

# ✅ 而是因為：
# - User A 執行了 POST /api/2.1/jobs/create
```

**為什麼選項這樣寫？**

這是考試的干擾設計：
- ✅ 結論正確（User A 永遠出現）
- ⚠️ 理由有誤導性（與 Notebook 所有權無關）
- 📝 測試考生是否理解真正的技術原理

---

## 🧠 記憶法與對比

### 口訣記憶
**「Creator 看建立，Run-as 看觸發，Owner 可轉移」**  
**「誰建的 Job 是 Creator，誰跑的 Run 是 Run-as」**

### Jobs API 關鍵欄位對比

| 欄位 | 層級 | 語意 | 可變性 | 範例情境 |
|------|------|------|--------|----------|
| **creator_user_name** | Job & Run | 誰建立了 Job | ❌ 不可變 | User A 建立 Job |
| **owner_user_name** | Job | 當前所有者 | ✅ 可變 | User C 取得所有權 |
| **run_as_user_name** | Run | 執行身份 | ✅ 每次 Run 可能不同 | User B 觸發執行 |
| **trigger** | Run | 觸發方式 | ✅ 每次 Run 可能不同 | API / Schedule / Manual |

### 角色識別決策樹

```
要識別什麼資訊？
│
├─ 誰建立了這個 Job？
│  └─ creator_user_name (不可變)
│
├─ 誰目前擁有這個 Job？
│  └─ owner_user_name (可變)
│
├─ 誰觸發了這次 Run？
│  └─ run_as_user_name (每次 Run 可能不同)
│
└─ 如何觸發這次 Run？
   └─ trigger (API / Schedule / Manual)
```

### 實務範例時間線

```
Timeline:

2024-01-01: User A 建立 Job
├─ creator_user_name = "user.a@company.com"  ← 固定
└─ owner_user_name = "user.a@company.com"    ← 初始值

2024-02-01: User C 取得 Owner 權限
├─ creator_user_name = "user.a@company.com"  ← 不變
└─ owner_user_name = "user.c@company.com"    ← 改變

2024-03-01: User B 透過 API 觸發 Run #1
├─ creator_user_name = "user.a@company.com"  ← 繼承自 Job
└─ run_as_user_name = "user.b@company.com"   ← 觸發者

2024-03-02: User B 透過 API 觸發 Run #2
├─ creator_user_name = "user.a@company.com"  ← 仍然是 User A
└─ run_as_user_name = "user.b@company.com"   ← 仍然是 User B

2024-03-03: User C 手動觸發 Run #3
├─ creator_user_name = "user.a@company.com"  ← 仍然是 User A
└─ run_as_user_name = "user.c@company.com"   ← 這次是 User C
```

---

## 💡 實務應用

### 查詢不同角色資訊

```python
import requests

base_url = "https://<databricks-instance>/api/2.1"
headers = {"Authorization": f"Bearer {token}"}

# 查詢 Job 資訊
job_info = requests.get(
    f"{base_url}/jobs/get",
    headers=headers,
    params={"job_id": 123}
).json()

print(f"Job Creator: {job_info['creator_user_name']}")
print(f"Current Owner: {job_info['settings'].get('owner_user_name', 'N/A')}")

# 查詢 Run 資訊
run_info = requests.get(
    f"{base_url}/jobs/runs/get",
    headers=headers,
    params={"run_id": 456}
).json()

print(f"Run Creator (inherited): {run_info['creator_user_name']}")
print(f"Run As (actual executor): {run_info.get('run_as_user_name', 'N/A')}")
print(f"Trigger Type: {run_info['trigger']}")
```

### 審計與追蹤

```python
def audit_job_ownership(job_id):
    """審計 Job 的所有權變更歷史"""
    
    # 1. 取得當前狀態
    job = requests.get(
        f"{base_url}/jobs/get",
        headers=headers,
        params={"job_id": job_id}
    ).json()
    
    print(f"📦 Job ID: {job_id}")
    print(f"🔨 Original Creator: {job['creator_user_name']}")
    print(f"👤 Current Owner: {job['settings'].get('owner_user_name', 'N/A')}")
    
    # 2. 查詢權限歷史（透過 audit logs）
    audit_logs = requests.get(
        f"{base_url}/workspace/get-status",
        headers=headers,
        params={"path": f"/Jobs/{job_id}"}
    ).json()
    
    # 3. 分析 Runs 的觸發者分布
    runs = requests.get(
        f"{base_url}/jobs/runs/list",
        headers=headers,
        params={"job_id": job_id}
    ).json()
    
    trigger_stats = {}
    for run in runs.get('runs', []):
        executor = run.get('run_as_user_name', 'Unknown')
        trigger_stats[executor] = trigger_stats.get(executor, 0) + 1
    
    print(f"\n📊 Trigger Statistics:")
    for user, count in trigger_stats.items():
        print(f"  - {user}: {count} runs")

# 使用範例
audit_job_ownership(123)

# 輸出：
# 📦 Job ID: 123
# 🔨 Original Creator: user.a@company.com
# 👤 Current Owner: user.c@company.com
#
# 📊 Trigger Statistics:
#   - user.b@company.com: 45 runs
#   - user.c@company.com: 3 runs
```

### 監控外部觸發的 Jobs

```python
def monitor_externally_triggered_jobs():
    """監控由外部系統觸發的 Jobs"""
    
    # 查詢所有 Jobs
    all_jobs = requests.get(
        f"{base_url}/jobs/list",
        headers=headers
    ).json()
    
    for job in all_jobs.get('jobs', []):
        job_id = job['job_id']
        creator = job['creator_user_name']
        
        # 查詢最近的 Runs
        runs = requests.get(
            f"{base_url}/jobs/runs/list",
            headers=headers,
            params={"job_id": job_id, "limit": 10}
        ).json()
        
        for run in runs.get('runs', []):
            if run['trigger'] == 'API':
                executor = run.get('run_as_user_name', 'Unknown')
                
                if executor != creator:
                    print(f"⚠️ Job {job_id} created by {creator}")
                    print(f"   but triggered by {executor} via API")
                    print(f"   Run ID: {run['run_id']}")
                    print()
```

---

## 📚 官方文件參考

1. **Jobs API - Get Job:**
   - [Databricks Docs - Jobs API Get](https://docs.databricks.com/api/workspace/jobs/get)

2. **Jobs API - Get Run:**
   - [Databricks Docs - Jobs Runs Get](https://docs.databricks.com/api/workspace/jobs/getruns)

3. **Job Permissions:**
   - [Databricks Docs - Job Permissions](https://docs.databricks.com/security/auth/access-control/jobs-acl.html)

4. **Audit Logs:**
   - [Databricks Docs - Audit Logs](https://docs.databricks.com/administration-guide/account-settings/audit-logs.html)

---

## 🎯 考試技巧

**遇到 API 欄位語意題目時：**

1. ✅ **理解欄位命名** → creator = 建立者，owner = 所有者，run_as = 執行者
2. ✅ **區分可變與不可變** → creator 不變，owner 可變
3. ✅ **注意欄位層級** → Job 層級 vs Run 層級

**關鍵判斷標準：**

| 欄位關鍵字 | 語意 | 可變性 |
|-----------|------|--------|
| **creator** | 建立者 | ❌ 不可變 |
| **owner** | 所有者 | ✅ 可轉移 |
| **run_as** | 執行者 | ✅ 每次 Run 可能不同 |
| **trigger** | 觸發方式 | ✅ 每次 Run 可能不同 |

**陷阱警示：**
- ⚠️ 不要混淆「建立 Job 的人」與「觸發 Run 的人」
- ⚠️ 不要被「取得 Owner 權限」誤導，以為 creator 會改變
- ⚠️ 選項 C 的理由（Notebook 所有權）有誤導性，但結論正確

**實務建議：**
- 使用 `run_as_user_name` 追蹤實際執行者
- 使用 audit logs 取得完整的操作歷史
- 設定 Service Principal 執行生產 Jobs，避免依賴個人 token
