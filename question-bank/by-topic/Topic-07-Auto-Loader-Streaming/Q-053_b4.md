# Question #053

---

## 題目資訊
### 題目編號
**ID:** `Q-053`
### 來源
**來源:** Community
### 難度等級
**難度:** `L3-Advanced`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A data engineer has the following logic to handle duplicates in Spark Structured Streaming:

```
(spark.readStream
       .table("bronze")
       .filter("topic = 'orders'")
       .select(F.from_json(F.col("value").cast("string"), schema).alias("v"))
       .select("v.*")
       .withWatermark("order_timestamp", "30 seconds")
       .dropDuplicates(["order_id", "order_timestamp"]))
```

However, they notice that this logic is not sufficient to prevent duplicates for events that arrive later than the watermark threshold.

Which of the following code snippets can the data engineer include in a `foreachBatch` function to completely handle streaming duplicates?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** ```
MERGE INTO orders_silver a
USING microbatch b
ON a.order_id=b.order_id AND a.order_timestamp=b.order_timestamp
WHEN NOT MATCHED THEN INSERT *
```
- **B.** ```
(spark.readStream
.table("microbatch")
.withWatermark("order_timestamp", "7 days")
.dropDuplicates(["order_id", "order_timestamp"]))
```
- **C.** ```
APPLY CHANGES INTO orders_silver a
FROM STREAM(microbatch)
KEYS (order_id, order_timestamp)
SEQUENCE BY order_timestamp
COLUMNS *
```
- **D.** ```
COPY INTO orders_silver
FROM microbatch
DISTINCT ALL
COPY_OPTIONS ('mergeSchema' = 'true');
```

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Streaming`, `Delta-MERGE`
### Trap Tags (陷阱類型標籤)
**Traps:** `Execution-Behavior`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Development

---

## 答案與來源
### 正確答案
**正解:** `A`
### 答案來源
- **來源標註答案:** A
- **社群共識:** A

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Streaming 去重 + foreachBatch + MERGE
**關鍵概念:** watermark 只能處理時間範圍內去重
**題目關鍵字：**
- **late events**: watermark 之外的重複
- **foreachBatch**: 批次 idempotent 寫入

---

## ✅ 正解說明
### 為什麼 A 是正確答案？
`dropDuplicates` 搭配 watermark 只能去除**水位線內**的重複；超過水位線的晚到資料無法去重。使用 `foreachBatch` 在每批次對目標表做 **MERGE 的 idempotent 寫入**，可避免跨批次與晚到資料造成重複。

---

## ❌ 錯誤選項排除
### B
**為什麼錯誤？**
- 仍是 streaming 內部去重，無法保證跨批次完全去重。

### C
**為什麼錯誤？**
- `APPLY CHANGES` 是 DLT 用法，不適用於 foreachBatch 片段。

### D
**為什麼錯誤？**
- COPY INTO 不提供去重保證。

---

## 🧠 記憶法
### 口訣
**「晚到去重靠 MERGE。」**

---

## 📚 官方文件
- [foreachBatch](https://spark.apache.org/docs/latest/structured-streaming-programming-guide.html#foreachbatch)
- [Delta MERGE](https://docs.databricks.com/en/delta/merge.html)

---

**[返回題目](#question-053)**
