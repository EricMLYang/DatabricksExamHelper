# Question #021

---

## 題目資訊

### 題目編號
**ID:** `Q-021`

### 來源
**來源:** Community

### 難度等級
**難度:** `L1-Basic`

---

## 題目內容

### 題幹

A data engineer uses the dynamic reference `{{job.start_time.iso_datetime}}` to configure the value of a task parameter in a job.

Which of the following statements correctly describes the timezone of the returned timestamp?

### 選項

- **A.** The timestamp is based on the workspace cloud region's local time.
- **B.** The timestamp is based on the cluster virtual machine's local time.
- **C.** The timestamp is in UTC.
- **D.** The timestamp is based on the user's local time who triggered the job.

---

## 標籤系統

### Topic Tags (技術主題標籤)
**Topics:** `Databricks-Workflows`

### Trap Tags (陷阱類型標籤)
**Traps:** `Concept-Confusion`

### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Operations

---

## 答案與來源

### 正確答案
**正解:** `C`

### 答案來源
- **來源標註答案:** C
- **社群共識:** C

---

# 題目解析

---

## 📍 考點識別

**核心技術:** Databricks Jobs / Dynamic References
**關鍵概念:** Job parameters, UTC timezone, Consistency

**題目關鍵字：**
- **dynamic reference**: 動態參數引用。
- **iso_datetime**: ISO 格式的日期時間。
- **timezone**: 時區設定。

---

## ✅ 正解說明

### 為什麼 C 是正確答案？

**技術原理：**

1. **一致性設計**:
   Databricks Jobs 中所有時間相關的動態引用都使用 **UTC 時區**，確保跨區域、跨叢集、跨用戶的一致性。

2. **可用的時間引用**:
   | 引用 | 說明 | 時區 |
   |------|------|------|
   | `{{job.start_time.iso_datetime}}` | ISO 格式日期時間 | UTC |
   | `{{job.start_time.iso_date}}` | ISO 格式日期 | UTC |
   | `{{job.start_time.iso_weekday}}` | 星期幾（1-7） | UTC |
   | `{{job.start_time.is_weekday}}` | 是否為工作日 | UTC |

3. **使用範例**:
   ```
   {{job.start_time.iso_datetime}}
   → "2024-01-15T08:30:00Z"  (Z 表示 UTC)
   ```

---

## ❌ 錯誤選項排除

### A - Workspace 雲端區域的本地時間

**為什麼錯誤？**

- **會造成不一致**: 不同區域的 workspace 會有不同結果。
- **UTC 確保一致性**: 使用 UTC 可避免跨區域問題。

### B - Cluster VM 的本地時間

**為什麼錯誤？**

- **VM 時間可能不同**: 不同 VM 可能有不同的時區設定。
- **不依賴 Cluster**: Job 參數在 Job 層級解析，不依賴特定 Cluster。

### D - 觸發 Job 的使用者本地時間

**為什麼錯誤？**

- **使用者位置不固定**: 同一使用者可能在不同時區操作。
- **排程任務無使用者**: 自動排程執行時沒有「觸發使用者」。

---

## 🧠 記憶法

### 口訣

**「Job 時間用 UTC，全球一致不混亂」**

### Job 動態引用一覽

| 類型 | 引用範例 | 說明 |
|------|----------|------|
| **時間** | `{{job.start_time.iso_datetime}}` | Job 開始時間 |
| **Job 資訊** | `{{job.id}}` | Job ID |
| **Run 資訊** | `{{job.run_id}}` | Run ID |
| **Task 資訊** | `{{task.name}}` | Task 名稱 |

---

## 📚 官方文件

- [Job Parameter References](https://docs.databricks.com/workflows/jobs/parameter-value-references.html)
- [Dynamic Value References](https://docs.databricks.com/workflows/jobs/parameter-value-references.html#supported-dynamic-value-references)

---

**[返回題目](#question-021)**
