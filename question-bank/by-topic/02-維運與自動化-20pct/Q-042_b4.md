# Question #042

---

## 題目資訊
### 題目編號
**ID:** `Q-042`
### 來源
**來源:** Community
### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A data engineer in a call center needs to implement an SQL alert to track ticket volume and status changes. They want to set the alert based on multiple columns of the tickets table. The alert should be triggered when both of the following conditions are met:

- The number of new tickets exceeds 200.
- The number of tickets under processing exceeds 150.

Which of the following SQL queries correctly implements this alert logic?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** ```
SELECT new_tickets, under_processing
FROM (
  SELECT
    SUM(CASE WHEN status = 'new' THEN 1 ELSE 0 END) AS new_tickets,
    SUM(CASE WHEN status = 'in_progress' THEN 1 ELSE 0 END) AS under_processing
  FROM tickets
) statistics
WHERE new_tickets > 200
AND under_processing > 150
```
- **B.** ```
SELECT new_tickets + under_processing
FROM (
  SELECT
    SUM(CASE WHEN status = 'new' THEN 1 ELSE 0 END) AS new_tickets,
    SUM(CASE WHEN status = 'in_progress' THEN 1 ELSE 0 END) AS under_processing
  FROM tickets
) statistics
WHERE new_tickets + under_processing > 350
```
- **C.** ```
SELECT
  SUM(CASE WHEN status = 'new' THEN 1 ELSE 0 END) AS new_tickets,
  SUM(CASE WHEN status = 'in_progress' THEN 1 ELSE 0 END) AS under_processing
FROM tickets
WHERE new_tickets > 200
AND under_processing > 150
```
- **D.** ```
SELECT CASE
  WHEN new_tickets > 200 AND under_processing > 150 THEN 1
    ELSE 0
  END
FROM (
  SELECT
    SUM(CASE WHEN status = "new" THEN 1 ELSE 0 END) AS new_tickets,
    SUM(CASE WHEN status = "in_progress" THEN 1 ELSE 0 END) AS under_processing
  FROM tickets
) statistics
```

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Databricks-SQL`, `Data-Quality`
### Trap Tags (陷阱類型標籤)
**Traps:** `Logical-Trap`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Development

---

## 答案與來源
### 正確答案
**正解:** `D`
### 答案來源
- **來源標註答案:** D
- **社群共識:** D

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Databricks SQL Alerts
**關鍵概念:** Alert 需要單一輸出欄位
**題目關鍵字：**
- **SQL alert**: 單一欄位判斷
- **multiple conditions**: 用 CASE 組合

---

## ✅ 正解說明
### 為什麼 D 是正確答案？
Databricks SQL Alert 只能判斷**單一欄位**，因此需先在子查詢計算統計，再用 `CASE WHEN` 產生單一結果欄位供 alert 評估。D 正確符合此邏輯。

---

## ❌ 錯誤選項排除
### A
**為什麼錯誤？**
- 回傳兩個欄位，不符合 alert 單欄位要求。

### B
**為什麼錯誤？**
- 以總和 > 350 不能保證兩條件都成立。

### C
**為什麼錯誤？**
- `WHERE` 不能直接用別名欄位，且仍非單一欄位輸出。

---

## 🧠 記憶法
### 口訣
**「Alert 只能看一欄，CASE 幫你合。」**

---

## 📚 官方文件
- [Databricks SQL alerts](https://docs.databricks.com/en/sql/user/alerts/index.html)

---

**[返回題目](#question-042)**
