# Question #009

---

## 題目資訊

### 題目編號
**ID:** `Q-009`

### 來源
**來源:** Community

### 難度等級
**難度:** `L1-Basic`

---

## 題目內容

### 題幹

Which of the following describes Cron syntax in Databricks Jobs?

### 選項

- **A.** It's an expression to represent the retry policy of a job.
- **B.** It's an expression to represent the run timeout of a job.
- **C.** It's an expression to represent the maximum concurrent runs of a job.
- **D.** It's an expression to represent complex job schedule that can be defined programmatically.

---

## 標籤系統

### Topic Tags (技術主題標籤)
**Topics:** `Databricks-Workflows`

### Trap Tags (陷阱類型標籤)
**Traps:** `Concept-Confusion`

### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Operations

---

## 答案與來源

### 正確答案
**正解:** `D`

### 答案來源
- **來源標註答案:** D
- **社群共識:** D

---

# 題目解析

---

## 📍 考點識別

**核心技術:** Databricks Jobs / Cron Scheduling
**關鍵概念:** Cron syntax, Job scheduling, Programmatic schedule definition

**題目關鍵字：**
- **Cron syntax**: Unix/Linux 傳統的時間排程表示法。
- **Databricks Jobs**: 工作排程與自動化。
- **Schedule**: 定義任務執行時間。

---

## ✅ 正解說明

### 為什麼 D 是正確答案？

**技術原理：**

1. **Cron Syntax 的用途**:
   Cron 是一種時間表示語法，源自 Unix 系統，用於定義任務的執行時間。在 Databricks Jobs 中，Cron syntax 用來定義複雜的排程規則。

2. **Cron 表達式結構**:
   ```
   ┌───────────── 秒 (0-59)
   │ ┌───────────── 分 (0-59)
   │ │ ┌───────────── 時 (0-23)
   │ │ │ ┌───────────── 日 (1-31)
   │ │ │ │ ┌───────────── 月 (1-12)
   │ │ │ │ │ ┌───────────── 星期 (0-6, 0=Sunday)
   │ │ │ │ │ │
   * * * * * *
   ```

3. **範例**:
   ```
   0 0 9 * * ?      # 每天早上 9:00
   0 0 */2 * * ?    # 每 2 小時
   0 30 8 ? * MON-FRI  # 週一到週五早上 8:30
   0 0 0 1 * ?      # 每月 1 號午夜
   ```

**Databricks 排程設定方式：**

| 方式 | 說明 | 適用場景 |
|------|------|----------|
| **UI 互動式** | 透過下拉選單設定 | 簡單排程 |
| **Cron Syntax** | 撰寫 Cron 表達式 | 複雜/精確排程 |
| **API/CLI** | 程式化設定 | 自動化部署 |

---

## ❌ 錯誤選項排除

### A - Retry policy (重試政策)

**為什麼錯誤？**

- **與 Cron 無關**: 重試政策是透過 `max_retries` 和 `min_retry_interval_millis` 等參數設定。
- **Cron 只管排程**: 不涉及失敗後的重試邏輯。

### B - Run timeout (執行逾時)

**為什麼錯誤？**

- **不同設定項**: 執行逾時透過 `timeout_seconds` 參數設定。
- **Cron 定義的是「何時開始」**: 不是「執行多久」。

### C - Maximum concurrent runs (最大並行執行數)

**為什麼錯誤？**

- **不同設定項**: 並行執行數透過 `max_concurrent_runs` 參數設定。
- **Cron 與並行無關**: Cron 只決定觸發時間點。

---

## 🧠 記憶法

### 口訣

**「Cron 定時間，六星排一行」**

- **Cron** 用於定義執行時間
- **六個欄位** 定義秒、分、時、日、月、週

### Cron 特殊字元

| 字元 | 意義 | 範例 |
|------|------|------|
| `*` | 任意值 | `* * * * * ?` = 每秒 |
| `?` | 不指定（日/週專用） | `0 0 9 ? * MON` |
| `/` | 間隔 | `*/15` = 每 15 單位 |
| `-` | 範圍 | `MON-FRI` = 週一到五 |
| `,` | 列舉 | `1,15` = 1 號和 15 號 |

### Databricks Job 設定項對照

| 設定項 | 參數名稱 | 說明 |
|--------|----------|------|
| **排程** | `schedule.quartz_cron_expression` | Cron 表達式 |
| 重試 | `max_retries` | 最大重試次數 |
| 逾時 | `timeout_seconds` | 執行逾時秒數 |
| 並行 | `max_concurrent_runs` | 最大並行數 |

---

## 📚 官方文件

- [Databricks Jobs Scheduling](https://docs.databricks.com/workflows/jobs/jobs.html#schedule-a-job)
- [Cron Syntax Reference](https://docs.databricks.com/workflows/jobs/jobs.html#cron-syntax)
- [Jobs API](https://docs.databricks.com/api/workspace/jobs)

---

**[返回題目](#question-009)**
