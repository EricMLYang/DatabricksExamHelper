# Question #081

---

## 題目資訊
### 題目編號
**ID:** `Q-081`
### 來源
**來源:** Official Practice Exam
### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A CHECK constraint has been successfully added to the Delta table named `activity_details` using the following logic:

```sql
ALTER TABLE activity_details
ADD CONSTRAINT valid_coordinates
CHECK (
  latitude >= -90 AND
  latitude <= 90 AND
  longitude >= -180 AND
  longitude <= 180);
```

A batch job is attempting to insert new records to the table, including a record where `latitude = 45.50` and `longitude = 212.67`.

Which statement describes the outcome of this batch insert?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** The write will fail when the violating record is reached; any records previously processed will be recorded to the target table.
- **B.** The write will fail completely because of the constraint violation and no records will be inserted into the target table.
- **C.** The write will insert all records except those that violate the table constraints; the violating records will be recorded to a quarantine table.
- **D.** The write will include all records in the target table; any violations will be indicated in the boolean column named `valid_coordinates`.
- **E.** The write will insert all records except those that violate the table constraints; the violating records will be reported in a warning log.

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Delta-Constraints`, `Delta-Lake`, `Data-Quality`
### Trap Tags (陷阱類型標籤)
**Traps:** `Concept-Confusion`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Data Quality

---

## 答案與來源
### 正確答案
**正解:** `B`
### 答案來源
- **來源標註答案:** B
- **社群共識:** B

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Delta Lake CHECK constraints
**關鍵概念:** 事務原子性、約束違反回滾
**題目關鍵字：**
- **CHECK constraint**: 寫入時驗證
- **batch insert**: 單一事務
- **violating record**: 觸發回滾

---

## ✅ 正解說明
### 為什麼 B 是正確答案？
Delta 的 CHECK 約束是**硬性約束**，寫入以單一事務提交：
- 只要**任一筆**違反約束，整個寫入**失敗回滾**
- 不會部分寫入，也不會自動隔離違反資料

本題的 `longitude = 212.67` 超出範圍，因此**整批失敗、零寫入**。

---

## ❌ 錯誤選項排除
### A. 部分成功
**為什麼錯誤？**
- 違反原子性，Delta 不允許部分成功

### C. 隔離到 quarantine
**為什麼錯誤？**
- Delta CHECK 沒有自動隔離表機制

### D. 以布林欄位標記
**為什麼錯誤？**
- 約束名稱不是欄位，且違反會直接失敗

### E. 只記警告
**為什麼錯誤？**
- 約束違反是錯誤，不是 warning

---

## 🧠 記憶法
### 口訣
**「CHECK 一犯，整批全翻」**

---

## 📚 官方文件
- [Delta Lake constraints](https://docs.databricks.com/delta/constraints.html)

---

**[返回題目](#question-081)**
