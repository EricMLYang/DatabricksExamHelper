# Question #019

---

## 題目資訊
### 題目編號
**ID:** `Q-019`
### 來源
**來源:** Community
### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
Given the following query:

```
spark.table("stream_sink")
        .filter("recent = true")
        .dropDuplicates(["item_id", "item_timestamp"])
    .write
        .mode ("overwrite")
        .table("stream_data_stage")
```

Which statement describes the result of executing this query ?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** An incremental job will overwrite the stream_sink table by those deduplicated records from stream_data_stage that have been added since the last time the job was run.
- **B.** A batch job will overwrite the stream_data_stage table by deduplicated records calculated from all "recent" items in the stream_sink table
- **C.** An incremental job will overwrite the stream_data_stage table by those deduplicated records from stream_sink that have been added since the last time the job was run.
- **D.** A batch job will overwrite the stream_data_stage table by those deduplicated records from stream_sink that have been added since the last time the job was run.

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `DataFrames`, `Delta-Lake`
### Trap Tags (陷阱類型標籤)
**Traps:** `Execution-Behavior`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Development

---

## 答案與來源
### 正確答案
**正解:** `B`
### 答案來源
- **來源標註答案:** B
- **社群共識:** B

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Batch vs Streaming 讀取行為
**關鍵概念:** `spark.table()` 是批次讀取
**題目關鍵字：**
- **spark.table**: 靜態讀取
- **overwrite**: 每次覆寫整張表

---

## ✅ 正解說明
### 為什麼 B 是正確答案？
`spark.table()` 讀取的是**靜態 snapshot**，不是串流來源。因此每次執行都會掃描 `stream_sink` 的當前完整資料，過濾 `recent = true`、去重後，再以 **overwrite** 模式覆寫 `stream_data_stage`。

---

## ❌ 錯誤選項排除
### A / C
**為什麼錯誤？**
- 沒有使用 `readStream`，所以不是 incremental job。

### D
**為什麼錯誤？**
- 雖然是 batch，但不是「只覆寫新增資料」，而是整張覆寫。

---

## 🧠 記憶法
### 口訣
**「spark.table 是批次；overwrite 每次全覆。」**

### 比較表
| API | 行為 |
|---|---|
| spark.table | batch snapshot |
| spark.readStream.table | streaming |

---

## 📚 官方文件
- [DataFrameReader.table](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.DataFrameReader.table.html)

---

**[返回題目](#question-019)**
