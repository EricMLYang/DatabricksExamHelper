# Question #038

---

## 題目資訊
### 題目編號
**ID:** `Q-038`
### 來源
**來源:** Community
### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A data engineer in an international school has implemented the following PySpark code:

```
from pyspark.sql.window import Window
from pyspark.sql.functions import avg, col
 
window_spec = Window.partitionBy("student_id").orderBy("exam_date")\
               .rowsBetween(Window.unboundedPreceding, Window.currentRow)
 
df_new = df_student_results.withColumn("avg_score", avg("score").over(window_spec))
```

Which of the following correctly describes what this code does?
### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** It adds a column showing the overall average score of each student, ordered by exam date.
- **B.** It adds a column showing the cumulative average score of each exam from the first enrolled student to and including the current student.
- **C.** It adds a column showing the cumulative average score of each student from their first exam up to and including the current exam.
- **D.** It adds a column showing the overall average score of each exam, regardless of student.

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `PySpark`, `DataFrames`
### Trap Tags (陷阱類型標籤)
**Traps:** `Execution-Behavior`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Development

---

## 答案與來源
### 正確答案
**正解:** `C`
### 答案來源
- **來源標註答案:** C
- **社群共識:** C

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Window function 累積平均
**關鍵概念:** partitionBy + orderBy + rowsBetween
**題目關鍵字：**
- **partitionBy("student_id")**: 以學生分區
- **unboundedPreceding to currentRow**: 累積窗口

---

## ✅ 正解說明
### 為什麼 C 是正確答案？
視窗以 `student_id` 分區並依 `exam_date` 排序，且範圍從第一筆到目前列，因此 `avg_score` 是**每位學生的累積平均分數**。

---

## ❌ 錯誤選項排除
### A
**為什麼錯誤？**
- 這不是單一整體平均，而是累積平均。

### B
**為什麼錯誤？**
- 不是跨所有學生的累積平均。

### D
**為什麼錯誤？**
- 不會跨學生計算整體平均。

---

## 🧠 記憶法
### 口訣
**「分學生、排日期、累積平均到當前。」**

---

## 📚 官方文件
- [Window functions](https://spark.apache.org/docs/latest/api/python/reference/pyspark.sql/api/pyspark.sql.Window.html)

---

**[返回題目](#question-038)**
