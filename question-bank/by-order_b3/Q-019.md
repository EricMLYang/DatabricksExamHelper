# Question #019

---

## 題目資訊
### 題目編號
**ID:** `Q-019`
### 來源
**來源:** Community
### 難度等級
**難度:** `L1-Basic`

---

## 題目內容
### 題幹
<!-- ⚠️ 保持英文原文，不要翻譯 -->
A data engineering team wants to automate job monitoring and improve observability by retrieving available jobs in the production Databricks workspace using REST API.

Which of the following REST API calls achieves this requirement?

### 選項
<!-- ⚠️ 保持英文原文，不要翻譯 -->
- **A.** Send POST request to the endpoint '/api/2.2/jobs/get'
- **B.** Send GET request to the endpoint '/api/2.2/jobs/list'
- **C.** Send GET request to the endpoint '/api/2.2/jobs/get'
- **D.** Send POST request to the endpoint '/api/2.2/jobs/list'

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Databricks-API`, `Databricks-Workflows`
### Trap Tags (陷阱類型標籤)
**Traps:** `Syntax-Confusion`
### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Operations

---

## 答案與來源
### 正確答案
**正解:** `B`
### 答案來源
- **來源標註答案:** B
- **社群共識:** B

---

# 題目解析

---

## 📍 考點識別
**核心技術:** Databricks REST API / Jobs API
**關鍵概念:** HTTP Method、API Endpoint
**題目關鍵字：**
- **retrieving available jobs**: 取得所有 Jobs
- **REST API**: REST API 呼叫
- **list**: 列出所有項目

---

## ✅ 正解說明
### 為什麼 B 是正確答案？

**GET /api/2.2/jobs/list** 是用來列出 workspace 中所有 jobs 的正確 API。

#### API 說明
| 項目 | 說明 |
|------|------|
| **HTTP Method** | GET |
| **Endpoint** | /api/2.2/jobs/list |
| **用途** | 列出所有 Jobs |
| **返回** | Jobs 列表 |

#### API 呼叫範例
```bash
# 使用 curl
curl -X GET \
  -H "Authorization: Bearer <token>" \
  "https://<workspace>/api/2.2/jobs/list"

# 使用 Python
import requests

response = requests.get(
    f"https://{workspace}/api/2.2/jobs/list",
    headers={"Authorization": f"Bearer {token}"}
)
jobs = response.json()["jobs"]
```

#### HTTP Method 選擇原則
```
RESTful API 設計原則：
├── GET → 讀取資料（不修改）
│   ├── list：列出所有項目
│   └── get：取得單一項目
├── POST → 建立/執行（會修改）
│   ├── create：建立新項目
│   └── run-now：執行 Job
├── PUT/PATCH → 更新
└── DELETE → 刪除
```

#### Jobs API 常用端點
| 端點 | Method | 用途 |
|------|--------|------|
| /api/2.2/jobs/list | **GET** | 列出所有 Jobs |
| /api/2.2/jobs/get | **GET** | 取得單一 Job 詳情 |
| /api/2.2/jobs/create | POST | 建立新 Job |
| /api/2.2/jobs/run-now | POST | 立即執行 Job |
| /api/2.2/jobs/delete | POST | 刪除 Job |

---

## ❌ 錯誤選項排除

### A. POST /api/2.2/jobs/get
**為什麼錯誤？**
- **Method 錯誤**：get 應該用 GET
- **端點錯誤**：get 是取得單一 Job，不是列出所有
- POST 通常用於修改操作

### C. GET /api/2.2/jobs/get
**為什麼錯誤？**
- **端點錯誤**：get 是取得單一 Job
- 需要提供 job_id 參數
- 不是列出所有 Jobs

```bash
# /jobs/get 的正確用法
curl -X GET \
  "https://<workspace>/api/2.2/jobs/get?job_id=123"
```

### D. POST /api/2.2/jobs/list
**為什麼錯誤？**
- **Method 錯誤**：list 是讀取操作，應該用 GET
- POST 通常用於修改或執行操作
- 違反 RESTful 設計原則

---

## 🧠 記憶法
### 口訣
**「讀資料用 GET，改資料用 POST」**

### REST API 設計慣例
```
常見端點模式：
├── GET  /items/list   → 列出所有
├── GET  /items/get    → 取得單一（需 ID）
├── POST /items/create → 建立新項目
├── POST /items/update → 更新項目
└── POST /items/delete → 刪除項目
```

### Databricks API 版本
```
API 版本演進：
├── 2.0 → 基本版本
├── 2.1 → 增強功能
└── 2.2 → 最新版本（推薦使用）
```

### API 呼叫檢查清單
```
檢查清單：
├── 正確的 HTTP Method？
│   ├── 讀取 → GET
│   └── 修改 → POST
├── 正確的 Endpoint？
│   ├── list → 列出多個
│   └── get → 取得單一
├── 認證 Header？
│   └── Authorization: Bearer <token>
└── 正確的 Workspace URL？
```

---

## 📚 官方文件
- [Jobs API - List](https://docs.databricks.com/api/workspace/jobs/list)
- [Jobs API - Get](https://docs.databricks.com/api/workspace/jobs/get)
- [Databricks REST API](https://docs.databricks.com/api/workspace/introduction)

---

**[返回題目](#question-019)**
