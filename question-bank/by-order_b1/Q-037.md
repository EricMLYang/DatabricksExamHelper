# Question #037

---

## 題目資訊
### 題目編號
**ID:** `Q-037`

### 來源
**來源:** Sample / Batch 1

### 難度等級
**難度:** `L2-Intermediate`

---

## 題目內容
### 題幹
Which of the following describes the minimal permissions a data engineer needs to edit the configurations of an existing cluster?

### 選項
- **A.** "Can Restart" privilege on the cluster

- **B.** Only administrators can edit the configurations on existing clusters

- **C.** Cluster creation allowed + "Can Manage" privileges on the cluster

- **D.** "Can Manage" privilege on the cluster

---

## 標籤系統
### Topic Tags (技術主題標籤)
**Topics:** `Security`, `Cluster-Permissions`, `Access-Control`

### Trap Tags (陷阱類型標籤)
**Traps:** `Over-Specification`

### Knowledge Domain (知識領域)
**Domain:** Data Engineering / Security & Access Control

---

## 答案與來源
### 正確答案
**正解:** `D`

### 答案來源
- **來源標註答案:** D
- **社群共識:** D

---

# 題目解析

---

## 📍 考點識別

**核心技術:** Databricks Cluster 權限管理

**關鍵概念:**
- Cluster-level permissions
- Can Manage 的功能範圍
- 最小權限原則（minimal permissions）

**題目關鍵字：**
- **minimal permissions**: 最小權限
- **edit the configurations**: 編輯設定
- **existing cluster**: 現有叢集

---

## ✅ 正解說明

### 為什麼 D 是正確答案？

**技術原理：**

編輯現有叢集的設定**只需要** `Can Manage` 權限，不需要額外的叢集建立權限（Allow cluster creation）。

#### Cluster 權限層級

| 權限層級 | 能力 |
|---------|------|
| **No Permissions** | 無法存取叢集 |
| **Can Attach To** | 可以將 notebook/job 附加到叢集 |
| **Can Restart** | 可以重啟叢集 |
| **Can Manage** | 可以**編輯設定**、終止、刪除叢集 |

#### Can Manage 的完整權限

```
Can Manage 權限包含：
├── 所有 Can Restart 的權限
│   └── 所有 Can Attach To 的權限
├── 編輯叢集設定（Edit configurations）✅
├── 終止叢集（Terminate）
├── 刪除叢集（Delete）
└── 管理叢集權限（Manage permissions）
```

#### 權限繼承關係

```
Can Manage
    ↓ 包含
Can Restart
    ↓ 包含
Can Attach To
    ↓ 包含
No Permissions
```

---

## ❌ 錯誤選項排除

### 選項 A
**"Can Restart" privilege on the cluster**

**為什麼錯誤？**

1. **Can Restart 不能編輯設定**：
   - Can Restart 只能重啟叢集
   - 無法修改叢集的配置

2. **Can Restart 的能力**：
   - 啟動/重啟叢集
   - 附加 notebook/job
   - **不包含**編輯設定

---

### 選項 B
**Only administrators can edit the configurations on existing clusters**

**為什麼錯誤？**

1. **過度限制**：
   - 不僅限於管理員
   - 任何有 `Can Manage` 權限的使用者都可以

2. **權限委派的目的**：
   - Databricks 設計權限系統就是為了讓非管理員也能管理資源
   - 管理員可以授予 `Can Manage` 給其他使用者

---

### 選項 C
**Cluster creation allowed + "Can Manage" privileges on the cluster**

**為什麼錯誤？**

1. **題目問的是「minimal permissions」**：
   - 「Cluster creation allowed」是**多餘的**
   - 編輯**現有**叢集不需要建立叢集的權限

2. **兩種權限的區別**：
   - `Allow cluster creation`：建立**新**叢集
   - `Can Manage`：管理**現有**叢集

3. **場景對比**：
```
編輯現有叢集設定 → 只需要 Can Manage
建立新叢集       → 需要 Allow cluster creation
```

---

## 🧠 記憶法

### 口訣
**「編輯設定找 Can Manage，建立新叢集才要 creation」**
→ 編輯現有叢集只需 Can Manage；建立新叢集才需要 Allow cluster creation

### Cluster 權限速查表

| 操作 | 所需最小權限 |
|------|-------------|
| 查看叢集 | Can Attach To |
| 附加 Notebook | Can Attach To |
| 重啟叢集 | Can Restart |
| **編輯設定** | **Can Manage** |
| 終止叢集 | Can Manage |
| 刪除叢集 | Can Manage |
| 建立新叢集 | Allow cluster creation |

### 權限設定方式

```
# 在 Cluster UI 中設定權限
Cluster → Edit Permissions → 選擇使用者/群組 → 選擇權限層級

# 或透過 API
POST /api/2.0/permissions/clusters/{cluster_id}
{
  "access_control_list": [
    {
      "user_name": "user@example.com",
      "permission_level": "CAN_MANAGE"
    }
  ]
}
```

### 常見權限組合場景

| 角色 | 建議權限 |
|------|---------|
| **資料工程師（管理叢集）** | Can Manage |
| **資料分析師（使用叢集）** | Can Attach To |
| **DevOps（維運叢集）** | Can Manage + Allow cluster creation |
| **初級開發者** | Can Attach To |

---

## 📚 官方文件

- [Cluster Access Control](https://docs.databricks.com/en/security/auth-authz/access-control/cluster-acl.html)
- [Compute Permissions](https://docs.databricks.com/en/compute/clusters-manage.html#cluster-level-permissions)

---

**[返回題目](#question-037)**
